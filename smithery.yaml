startCommand:
  type: http
  configSchema:
    type: object
    properties: {}
  commandFunction: |
    (config) => ({url: 'https://rumblingb.github.io/token-saver-pro'})

displayName: "Token Saver Pro Premium"
description: "Premium access to Token Saver Pro — bulk processing, history, CSV export, and API access for token optimization."
categories:
  - "AI"
  - "Tokens"
  - "Cost-Saving"
